{
  "name": "ai-safety-studio",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:stories": "node scripts/render-stories.mjs",
    "prebuild": "npm run generate:stories",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "vite": "^6.2.0"
  }
}
